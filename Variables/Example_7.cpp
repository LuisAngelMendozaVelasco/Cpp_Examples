//Declare many variables of the same type with a comma-separated list

#include <iostream>
#include "../myFunctions.h"
using namespace std;

int main() {
    int x = 5, y = 6, z = 50;

    cout << x + y + z << endl;

    askOS();
    return 0;
}